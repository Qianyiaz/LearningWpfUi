<Page x:Class="Wpf.Ui.Views.Pages.BrowserPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:pages="clr-namespace:WpfUi.ViewModels.Pages">

    <Page.DataContext>
        <pages:Page2ViewModel />
    </Page.DataContext>
    
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition 
                    Height="Auto"/>
                <RowDefinition 
                    Height="Auto"/>
                <RowDefinition />
            </Grid.RowDefinitions>
            <ui:CardControl
                Margin="8,8,8,0"
                Visibility="{Binding CardVisibility}">
                <ui:CardControl.Header>
                    <ui:TextBlock
                        Margin="15,0,0,0"
                        FontTypography="Subtitle"
                        Text="A simple browser" />
                </ui:CardControl.Header>
                <ui:Button
                    Command="{Binding HideCommand}">
                    <ui:Button.Icon>
                        <ui:SymbolIcon
                            FontSize="20"
                            Symbol="Dismiss48" />
                    </ui:Button.Icon>
                </ui:Button>
            </ui:CardControl>
            <ui:TextBox
                Grid.Row="1"
                Icon="{ui:SymbolIcon Search48}"
                Margin="8,8,8,0"
                Text="{Binding InputUrl,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                PlaceholderText="Search..." />
            <wv2:WebView2
                Grid.Row="2"
                Margin="8,0,8,8"
                Source="{Binding Url,Mode=TwoWay}" />
        </Grid>
</Page>